{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "outDir": "./dist",
    "rootDir": "./src",
    // "composite": false, // Remove
    "declaration": true, // Keep for tsup dts: true
    "declarationMap": true, // Keep for tsup dts: true
    // "emitDeclarationOnly": false, // Remove
    // "declarationDir": "./dist", // Remove
    "types": ["node", "vitest/globals"] // Keep vitest types
  },
  "include": ["src/**/*.ts"], // Keep include
  "exclude": ["node_modules", "dist", "src/**/*.test.ts"], // Keep exclude test files
  // Remove references
}