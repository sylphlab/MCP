{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "outDir": "./dist",
    "rootDir": "./src",
    "baseUrl": ".", // Keep baseUrl? Maybe remove. Let's remove for consistency.
    // "composite": false, // Remove
    "declaration": true, // Keep for tsup dts: true
    "declarationMap": true // Keep for tsup dts: true
    // "emitDeclarationOnly": false, // Remove
    // "declarationDir": "./dist" // Remove (tsup handles output)
  },
  "include": ["src/**/*.ts"],
  "exclude": ["node_modules", "dist"]
  // Remove references
}
